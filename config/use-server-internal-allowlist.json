{
  "description": "DÍVIDA, não permissão. Server actions que ainda aceitam flag `internal`. Em arquivo 'use server' todo export é endpoint HTTP → a flag é escolhida pelo CLIENTE, não pelo servidor. Esta lista só pode ENCOLHER: para tirar a flag, extraia um núcleo sem gate pra lib/ (modelo: lib/blueprint/core.ts) e deixe a action sempre gatada. NÃO adicione entradas para fazer o CI passar — a entrada nova é justamente o bug que este guard existe pra pegar. Contexto: CLAUDE.md, seção 'Server Actions são endpoints HTTP'.",
  "risco": {
    "actions/whatsapp.ts": "MAIOR RISCO. Bypass boolean, sem revalidação de tenant. Se o id vazar pro bundle (basta um client component importar), vira relay de WhatsApp arbitrário pelo número da Vertho → ban do número mata o canal de TODOS os tenants. Hoje o id NÃO está publicado (verificado por action-id-exposure.test.ts).",
    "actions/avaliacao-acumulada.ts": "Forma `{ empresaId }` — REVALIDA o tenant, então não é cross-tenant. Pula só o gate de permissão.",
    "actions/evolution-report.ts": "Forma `{ empresaId }` — REVALIDA o tenant. Pula só o gate de permissão."
  },
  "removidos": {
    "actions/fase1.ts::rodarIA2 + ::rodarIA3Uma, actions/fase3.ts::rodarIA4": "Removidos 10/07: o guard de bundle (action-id-exposure.test.ts) provou que os 3 tinham o action id PUBLICADO no JS do browser E o servidor aceitava — bypass explorável sem sessão, não latente. Zero callers usavam a flag: deleção pura."
  },
  "allowlist": [
    "actions/whatsapp.ts::enviarWhatsApp",
    "actions/whatsapp.ts::enviarAudio",
    "actions/avaliacao-acumulada.ts::gerarAvaliacaoAcumulada",
    "actions/avaliacao-acumulada.ts::gerarAvaliacaoAcumuladaParcial",
    "actions/evolution-report.ts::gerarEvolutionReport"
  ]
}
