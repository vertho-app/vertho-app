// A extração de material (PDF/DOCX) estrutura vários módulos-base via IA numa
// server action — dá folga de tempo (paralelizada, mas ainda pode levar minutos).
export const maxDuration = 300;

export default function ExtracaoVideoVerthoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
