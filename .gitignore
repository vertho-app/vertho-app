# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# GCP service-account keys (NUNCA comitar)
trigger-key.json
scripts/trigger-key.json
*-sa-key.json
gcp-*.json

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# script outputs (podem conter PII de migrações ou dumps de dados)
/outputs/
*-dump.json

# radar empresas — dados gerados (Parquet/temp DuckDB nunca vão pro git)
/data-pipeline/radarempresas/out/
/data-pipeline/radarempresas/tmp_duck/
/data-pipeline/**/raw/
*.parquet

# env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# playwright
test-results/
playwright-report/
playwright/.auth/

# backups locais
*.zip

# MCP server config (contém API keys)
.mcp.json

# Agent / assistant local state (contém preferências pessoais, logs e artefatos locais)
/.agents/
/.claude/
/.codex/

# python bytecode
__pycache__/
*.pyc

data-pipeline/radarempresas/br/out/

data-pipeline/radarempresas/**/_vt/
*.COMT

# logs de pipeline / execução
*.log

# microdados/temp pesados (ENEM etc) — nunca vão pro git
.tmp_*/

# backups locais (podem conter PII de colaboradores)
/backups/

/spike-bundle/
