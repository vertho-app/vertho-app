{
  "description": "Mutações RAW (update/delete sem tenantDb e sem predicado de tenant no WHERE) em tabelas tenant-owned/mistas. ZERADA em 03/07/2026 — toda mutação nova precisa de .eq('empresa_id', ...) (tenant) OU .is('empresa_id', null) (catálogo global) OU tenantDb. Allowlist vazia é o estado normal; adicionar entrada exige justificativa em code review.",
  "allowlist": {}
}
